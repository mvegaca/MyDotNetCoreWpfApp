<Page
    x:Class="MyDotNetCoreWpfApp.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    Style="{DynamicResource MetroPage}"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800"
    Title="SecondaryPage">
    <StackPanel
            x:Name="ContentArea"
            Margin="{StaticResource MediumLeftTopRightBottomMargin}">
        <TextBlock Text="Settings" />

        <TextBlock Text="Personalization" Margin="{StaticResource SmallTopMargin}" />
        <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
            <TextBlock Text="Choose Theme" />
            <StackPanel Margin="{StaticResource XSmallTopMargin}">
                <RadioButton
                    FontSize="15"
                    GroupName="AppTheme"
                    Content="Light"
                    IsChecked="{Binding IsLightThemeSelected}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding SetThemeCommand}" CommandParameter="Light.Blue" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </RadioButton>
                <RadioButton
                    Content="Dark"
                    FontSize="15"
                    GroupName="AppTheme"
                    Margin="{StaticResource XSmallTopMargin}"
                    IsChecked="{Binding IsDarkThemeSelected}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding SetThemeCommand}" CommandParameter="Dark.Blue" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </RadioButton>
            </StackPanel>
        </StackPanel>

        <TextBlock Text="About this application" Margin="{StaticResource SmallTopMargin}" />
        <StackPanel Margin="{StaticResource XSmallTopMargin}">
            <TextBlock Text="{Binding VersionDescription, Mode=OneWay}" />
            <TextBlock
                Text="Settings page placeholder text. Your app description goes here."
                Margin="{StaticResource XSmallTopMargin}" />
            <TextBlock Text="Privacy Statement">
                    <Hyperlink NavigateUri="https://YourPrivacyUrlGoesHere/"/>
            </TextBlock>
        </StackPanel>
    </StackPanel>
</Page>
